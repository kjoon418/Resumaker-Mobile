package com.resumaker.app.di

import org.koin.dsl.module

/**
 * ViewModel 정의 모듈.
 * 예: viewModel { LoginViewModel(get()) }
 */
val viewModelModule = module {
    // ViewModel 추가 시: viewModel { LoginViewModel(get()) }
}
